import React, { createContext, Component, useState, setState } from "react";
import PropTypes from "prop-types";

export const AccountContext = createContext({});

export const LoadingContext = createContext({});
